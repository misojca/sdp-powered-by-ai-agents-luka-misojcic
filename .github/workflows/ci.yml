name: CI

on:
  push:
    branches: [master, 'feature/**', 'fix/**', 'hotfix/**', 'chore/**', 'feat/**']
    paths:
      - 'src/**'
      - 'tests/**'
      - 'Dockerfile'
      - 'package*.json'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [master]
    paths:
      - 'src/**'
      - 'tests/**'
      - 'Dockerfile'
      - 'package*.json'
      - '.github/workflows/ci.yml'

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t kata-tests .

      - name: Run tests
        run: docker run --rm kata-tests
